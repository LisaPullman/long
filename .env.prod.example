# Copy to `.env` on your Ubuntu VPS (do not commit real secrets).
#
# Required
POSTGRES_PASSWORD=change-me-to-a-strong-password
JWT_SECRET=change-me-to-a-long-random-string

# Optional (defaults shown)
POSTGRES_USER=postgres
POSTGRES_DB=vanmart

# If you run a reverse proxy (Caddy/Nginx) on the host, keep FRONTEND_BIND_ADDR=127.0.0.1
# so users can only access the site via HTTPS.
FRONTEND_BIND_ADDR=127.0.0.1
FRONTEND_PORT=8080

# Backend listens inside Docker network; do NOT expose it publicly on the VPS.
BACKEND_PORT=3001

# If you need CORS (e.g. separate frontend domain), set an explicit origin.
# Example: https://example.com
# CORS_ORIGIN=

# Reverse proxy / rate limit (defaults are fine)
TRUST_PROXY=1
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=300
